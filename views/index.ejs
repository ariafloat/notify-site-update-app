<!DOCTYPE html>
<html>
  <head>
    <title>Welcome to Ariafloat Dream!</title>
    <meta name="description" content="Ariafloat Dream!">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <header>
      <h1>
        A Dream of the Future
      </h1>
    </header>
    <main>
      クローリング日時：<%= update %>
      <h2>ラクオリア創薬</h2>
      <ul>
      <% for (let item of raqualia) { %>
        <li><%= item.date %> <a href=" <%= item.url %>" target="_blank" rel="nofollow noopener"><%= item.title %></a></li>
      <% } %>
      </ul>
      <h2>Askat</h2>
      <ul>
      <% for (let item of askat) { %>
        <li><%= item.date %> <a href=" <%= item.url %>" target="_blank" rel="nofollow noopener"><%= item.title %></a></li>
      <% } %>
      </ul>
      <h2>Syros</h2>
      <ul>
      <% for (let item of syros) { %>
        <li><%= item.date %> <a href=" <%= item.url %>" target="_blank" rel="nofollow noopener"><%= item.title %></a></li>
      <% } %>
      </ul>
      <h2>Luoxin</h2>
      <ul>
      <% for (let item of luoxin) { %>
        <li><%= item.date %> <a href=" <%= item.url %>" target="_blank" rel="nofollow noopener"><%= item.title %></a></li>
      <% } %>
      </ul>
    </main>
  </body>
</html>
